---
name: domain-layer-test
description: This skill should be used when designing, refining, reviewing, or implementing domain-layer tests for this repository in DDD x Clean Architecture. It creates a table-first test case matrix from user requirements and `@docs` or `docs/domain`, revises the matrix based on feedback, and only generates Go test code after explicit approval. Use when the user mentions `internal/domain`, domain entities, value objects, aggregates, domain services, domain rules, ドメイン層テスト, テスト項目表, or 承認後にテストコード化.
---

# ドメイン層テスト

## いつ使うか

- `internal/domain` のテストを新規作成したいとき
- ドメイン層のテスト項目表を、ユーザー入力と `@docs` / `docs/domain` から起こしたいとき
- 値オブジェクト、エンティティ、集約、ドメインサービスのテスト観点を整理したいとき
- 「どこまでが domain test で、どこからが usecase / infra test か」を切り分けたいとき
- 先にテスト項目表を合意してから、Go のテストコードへ落としたいとき

## このスキルの対象

- このリポジトリの **DDD x クリーンアーキテクチャにおけるドメイン層テスト**
- `internal/domain` 配下に置く Go のテスト
- `docs/domain/ubiquitous-language.md`
- `docs/domain/usecase/`
- `docs/domain/uml/`

このスキルは、repository 実装の結合テストや HTTP の request/response テストを主対象にしない。  
それらが主題なら `infra-layer` や `presentation-layer`、`usecase-layer` の判断を優先する。

## 期待する出力

- 第 1 段階では、**テスト項目表** を返す
- 第 2 段階では、ユーザー指摘を反映した **修正版の項目表** を返す
- 第 3 段階では、承認済み項目表に沿った **Go のテストコード** を作成する
- 各段階で、**対象、観点、前提、期待結果、境界の判断** を短く明示する

## 基本原則

- **先に表、後でコード**
- **ドメイン層テストは pure なユニットテストを第一候補にする**
- **単一集約の不変条件、状態遷移、値検証、再構成を優先して拾う**
- **観点は「正常系、異常系、境界値、状態遷移、不変条件、正規化、再構成、回帰」を基本セットにする**
- **副作用、呼び順、トランザクション、外部 API 呼び出しは usecase 側を疑う**
- **モック前提の設計にしない。必要なら小さな fake を優先する**
- **項目表は「入力分類」より「守るべきルール」が見える形にする**
- **ユーザーの明示承認があるまで、テストコードは作らない**

## 最初に確認する

1. ユーザーが求めているのは **項目表の作成** か **コード生成** か
2. 対象は値オブジェクト、エンティティ、集約、ドメインサービスのどれか
3. `@docs` や `docs/domain` に、用語、状態、ユースケース、図があるか
4. それは本当に domain test か。usecase / infra の責務が混ざっていないか
5. 既存の実装やテストがあるなら、そこに命名やケース粒度の流儀があるか

必要情報が不足する場合は、推測で埋め切らず、前提を明示して確認する。

## 作業の入口

### 1. 項目表を作る

- まず `AGENTS.md` と `ARCHITECTURE.md` を確認する
- 局所的な値オブジェクトの相談などで、`docs` や境界判断に影響しない場合は、確認範囲を絞ってよい。その場合は前提に明記する
- 必要に応じて `../domain-layer/SKILL.md` と `../go-implementation/SKILL.md` を確認する
- ユーザー入力、添付ファイル、`@docs`、`docs/domain/`、対象コードから材料を集める
- 下記の 8 観点を基準に、必要な観点だけを洗い出す

必要なら、同値性や冪等性は上の 8 分類のどこに属するかを明示したうえで、
補助観点として備考列や補足へ出してよい。

### 2. 項目表を修正する

- ユーザー指摘を受けたら、表の行を追加、削除、統合、分割する
- 修正時は「何を変えたか」を短く添える

### 3. テストコードを作る

- 承認済み項目表を source of truth として扱う
- テーブル駆動テストを基本にする
- 1 ケース 1 ルールを意識し、過剰なケース統合を避ける
- domain service に依存がある場合も、まず fake で閉じられないかを確認する
- 依存が増えすぎる場合は、domain ではなく usecase 責務の可能性を先に報告する

### 承認の判定

- 次段階へ進む条件は、**直前に提示した項目表に対する明示承認** があること
- 明示承認の例: `OK`, `いいよ`, `進めて`, `LGTM`, `これでテストコードを書いて`, `表はこれでよい`
- 表を修正したあとは、**修正版に対して改めて承認** を取る
- いきなりコード作成を求められても、直前の項目表が未承認なら、項目表を再提示または要約して確認する
- 承認が曖昧なら、コード化へ進まず 1 文で確認する

## 項目表の作り方

### 推奨カラム

次の列を基本形とする。

| No | 対象 | 観点 | 事前状態/入力 | 操作 | 期待結果 | 備考 |
|---|---|---|---|---|---|---|
| 1 |  |  |  |  |  |  |

### 観点の優先順位

基本の観点分類は次の 8 つとする。

1. 正常系
2. 異常系
3. 境界値
4. 状態遷移
5. 不変条件
6. 正規化
7. 再構成
8. 回帰

対象に関係ない観点は省略してよい。  
ただし、使わない観点と使う観点が読んで分かるようにする。

ここでいう「優先」は、**どの観点を省略しにくいか** を示す目安であり、
表の行順そのものを決めるルールではない。

- 値オブジェクト: 正常系、異常系、境界値、正規化、回帰を優先し、必要なら同値性を補足する
- エンティティ: 正常系、異常系、状態遷移、不変条件、回帰を優先する
- 集約: 正常系、異常系、状態遷移、不変条件、再構成、回帰を優先する
- ドメインサービス: 正常系、異常系、不変条件相当の業務判定、回帰を優先し、usecase 責務が混ざらないかを確認する

### 表の出し方

- 先に 1 行で対象と前提を書く
- その下に Markdown 表を置く
- 表の行は、基本として **正常系 → 異常系 → 境界値 → 状態遷移 → 不変条件 → 正規化 → 再構成 → 回帰** の順にまとめる
- 表の行順は **観点の分類順を優先** し、同じ観点の中では同じ操作や同じ生成入口ごとに寄せて並べる
- 表の下に、今回使った観点の意味を 1 行ずつ短く補足してよい
- 表の後に、**domain / usecase の境界で除外した項目** があれば短く書く

### 観点の短い説明

表の下に補足を書く場合は、次のように短く説明する。

- 正常系: 業務上想定された正しい入力や状態で、期待した結果になること
- 異常系: 不正な入力や禁止された状態で、適切に失敗すること
- 境界値: 最小値、最大値、直前直後など、境目で挙動が変わりやすい条件を確かめること
- 状態遷移: ある状態から別の状態へ、許可された遷移だけが行えること
- 不変条件: 操作の前後を通じて、常に守られるべき業務ルールが壊れないこと
- 正規化: 入力値を trim、小文字化、形式統一などで期待する表現へ整えること
- 再構成: 新規生成とは別に、永続化済みデータから安全に復元できること
- 回帰: 過去バグや仕様誤解が再発しないこと

## コード生成のルール

- 先に表との対応関係を確認する
- テスト名は業務ルールが読める形にする
- `test1`, `case1` のような名前は使わない
- エラーメッセージ全文への過剰依存は避け、必要なら sentinel error や `errors.Is` を使う
- domain object の内部表現ではなく、公開 API と振る舞いを優先して確認する
- 副作用や呼び順の検証に寄り始めたら、domain test として適切かを見直す

## してはいけないこと

- いきなりテストコードを書き始めること
- 8 分類を機械的にすべて埋め、対象に不要な観点まで増やすこと
- `正常系 / 異常系 / 境界値` だけで表を埋め、業務ルールを見えなくすること
- usecase や infra の責務を domain test の項目表へ混ぜること
- モックの回数確認を中心にした brittle な domain test を作ること
- repository 実装や SQL の正しさを domain test に背負わせること
- ユーザー承認前に「表は仮でいいから」とコード化へ進むこと

## 回答テンプレート

### 項目表を返すとき

1. **対象**: 何のテスト項目表か
2. **前提**: どの情報を根拠にしたか
3. **項目表**: Markdown 表
4. **観点の説明**: 今回使った観点の意味を短く補足
5. **補足**: 除外した責務、未確定事項、確認したい点

### 修正版を返すとき

1. **変更点**: 何を追加、削除、修正したか
2. **修正版の項目表**
3. **観点の説明**: 必要なら更新後の観点を短く補足
4. **確認事項**: 必要なら 1 つか 2 つ

### コード生成に進むとき

1. 承認済み項目表との対応を短く示す
2. どのファイルへ何のテストを作るか示す
3. 実装後に、表の各観点をどこまでカバーしたかを短く報告する

## 追加資料

- ドメイン層の責務とテスト対象の切り分け: [../domain-layer/SKILL.md](../domain-layer/SKILL.md)
- Go でのテスト方針: [../go-implementation/SKILL.md](../go-implementation/SKILL.md)
- 例: [examples.md](examples.md)
